using UnityEngine;
using System.Collections.Generic;

// 각 유닛 인스턴스의 "원본 스탯"을 저장하여 강화 시 기준으로 사용
public class UnitEnhancementSuppoter : MonoBehaviour
{
    /*public bool Initialized;

    public int BaseDamage;
    public int BaseMoveSpeed;
    public int BaseAttackDelay;

    public void SaveBaseStatus(UnitModel model)
    {
        BaseDamage = model.Damage;
        BaseMoveSpeed = model.MoveSpeed;
        BaseAttackDelay = model.AttackDelay;

        Initialized = true;
    }*/
}